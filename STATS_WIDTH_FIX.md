# 筛选条件宽度修复

## 问题

筛选条件的宽度比下方图表卡片窄，视觉上不对齐。

## 原因分析

### 容器结构

```
.stats-container
  padding: 48rpx 40rpx 0
  ├── .header (标题)
  ├── .stats-cards (统计卡片)
  │   margin-bottom: 48rpx
  ├── .time-filter-section (筛选条件)
  │   margin: 24rpx 32rpx  ← 问题所在
  └── .chart-section (图表卡片)
      margin-bottom: 48rpx
```

### 宽度计算

**图表卡片实际宽度：**

- 容器宽度 - 容器左右padding
- = 100% - 40rpx - 40rpx
- = 100% - 80rpx

**筛选条件实际宽度（修复前）：**

- 容器宽度 - 容器左右padding - 自身左右margin
- = 100% - 40rpx - 40rpx - 32rpx - 32rpx
- = 100% - 144rpx
- **比图表卡片窄 64rpx**

## 解决方案

移除筛选条件的左右margin，只保留底部margin：

```less
/* 修复前 */
.time-filter-section {
  margin: 24rpx 32rpx; // 上下24rpx，左右32rpx
  ...;
}

/* 修复后 */
.time-filter-section {
  margin-bottom: 48rpx; // 只保留底部margin
  ...;
}
```

## 修复后的宽度

所有元素现在宽度一致：

```
┌────────────────────────────────────────┐
│ 统计卡片                                │
│ [总价值] [本月新增] [记录天数]          │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 筛选条件                                │
│ [按月▼]        [2024/1▼] - [2025/1▼]  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 图表1：物品统计                         │
│ [图表内容]                              │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 图表2：物品总值趋势                     │
│ [图表内容]                              │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 图表3：价值分布                         │
│ [图表内容]                              │
└────────────────────────────────────────┘
```

## 统一的间距规范

现在所有卡片元素遵循统一的间距规范：

1. **左右对齐**

   - 所有卡片左右边缘对齐
   - 由容器的 `padding: 48rpx 40rpx 0` 统一控制

2. **底部间距**

   - 统计卡片：`margin-bottom: 48rpx`
   - 筛选条件：`margin-bottom: 48rpx`
   - 图表卡片：`margin-bottom: 48rpx`
   - 保持一致的垂直间距

3. **内边距**
   - 筛选条件：`padding: 24rpx 32rpx`（较小，因为内容简单）
   - 图表卡片：`padding: 48rpx`（较大，因为内容丰富）

## 视觉效果

修复后的效果：

- ✅ 所有卡片宽度一致
- ✅ 左右边缘完美对齐
- ✅ 视觉上更整洁统一
- ✅ 符合设计规范

## 代码修改

### pages/stats/index.less

```less
/* 修改前 */
.time-filter-section {
  margin: 24rpx 32rpx;
  padding: 24rpx 32rpx;
  background: #ffffff;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

/* 修改后 */
.time-filter-section {
  margin-bottom: 48rpx; // 只保留底部margin
  padding: 24rpx 32rpx;
  background: #ffffff;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}
```

## 验证

- ✅ 无诊断错误
- ✅ 宽度与图表卡片一致
- ✅ 保持原有功能
- ✅ 视觉对齐完美
