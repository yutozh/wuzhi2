# 统计页面重新设计

## 更新内容

### 1. 统计粒度和时间范围选择

新增了统计粒度选择功能，支持：

- **按年统计**：默认显示近3年数据
- **按月统计**：默认显示近12个月数据（最长不超过12个月）

时间范围可以自定义选择：

- 按年：选择开始年份和结束年份
- 按月：选择开始年月和结束年月

### 2. 三个图表重新设计

#### 图表1：物品统计（柱状图）

- 显示在选定时间范围内，每个时间段购入的物品统计
- 右上角可切换"按数量"/"按价值"
- 按数量：显示每个时间段购入的物品数量
- 按价值：显示每个时间段购入的物品总价值

#### 图表2：物品总值趋势（折线图）

- 显示所有物品的累计趋势
- 右上角可切换"按数量"/"按价值"
- 按数量：显示物品数量的累计增长趋势
- 按价值：显示物品价值的累计增长趋势

#### 图表3：价值分布（饼图）

- 按物品类别显示分布情况
- 右上角可切换"按数量"/"按价值"
- 按数量：显示各类别物品数量占比
- 按价值：显示各类别物品价值占比

### 3. 数据结构

```javascript
data: {
  // 统计粒度
  granularity: 'month', // 'year' 或 'month'

  // 时间范围（按年）
  startYear: 2022,
  endYear: 2024,

  // 时间范围（按月）
  startYearMonth: '2023-12',
  endYearMonth: '2024-11',

  // 图表类型
  chart1Type: 'count', // 'count' 或 'value'
  chart2Type: 'count',
  chart3Type: 'count',

  // 图表数据
  chart1Data: [], // 物品统计数据
  chart2Data: [], // 累计趋势数据
  chart3Data: [], // 分布数据
}
```

### 4. 主要功能

- `onGranularityChange()`: 切换统计粒度
- `onChart1TypeChange()`: 切换图表1显示类型
- `onChart2TypeChange()`: 切换图表2显示类型
- `onChart3TypeChange()`: 切换图表3显示类型
- `onStartYearChange()`: 选择开始年份
- `onEndYearChange()`: 选择结束年份
- `onStartYearMonthChange()`: 选择开始年月
- `onEndYearMonthChange()`: 选择结束年月
- `checkMonthRange()`: 检查月份范围是否在12个月内

### 5. 样式保持

- 保留马卡龙配色风格
- 保持原有的卡片设计和布局
- 图表样式与之前保持一致

### 6. 交互优化

- 所有选择器都使用 picker 组件
- 图表类型切换使用点击切换
- 时间范围选择有合理性校验
- 按月统计时限制最长12个月范围

## 使用说明

1. 页面加载时自动初始化为按月统计，显示近12个月数据
2. 可以通过顶部的统计粒度选择器切换按年/按月
3. 切换粒度后，时间范围会自动重置为默认值
4. 每个图表右上角都有"按数量/按价值"切换按钮
5. 点击切换按钮可以在数量和价值视图之间切换
6. 图表会根据选择的时间范围和统计类型动态更新

## 技术实现

- 使用 VChart 图表库
- 数据按时间段聚合统计
- 图表2实现累计值计算
- 图表3实现百分比计算
- 时间范围校验确保数据合理性
