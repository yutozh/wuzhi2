# 筛选条件紧凑布局优化

## 优化内容

将统计页面的筛选条件从两行改为一行紧凑展示，去掉了不必要的描述文字。

## 改进前后对比

### 改进前

```
统计粒度    [按月 ▼]

时间范围    [2024年1月 ▼]  至  [2025年1月 ▼]
```

- 占用两行
- 有"统计粒度"、"时间范围"标签
- 分隔符使用"至"

### 改进后

```
[按月 ▼]    [2024/1 ▼]  -  [2025/1 ▼]
```

- 只占一行
- 去掉所有标签文字
- 分隔符改为"-"
- 月份格式简化为"年/月"

## 布局设计

### 结构

```
┌─────────────────────────────────────────┐
│ [按月▼]        [2024/1▼] - [2025/1▼]   │
└─────────────────────────────────────────┘
```

- 左侧：统计粒度选择器
- 右侧：时间范围选择器（两个选择器 + 分隔符）
- 使用 `justify-content: space-between` 分布

## 样式特点

### 容器

- `.filter-row-compact` - 主容器
  - `display: flex`
  - `justify-content: space-between`
  - `gap: 24rpx`

### 时间范围区域

- `.time-range-compact` - 时间范围容器
  - `display: flex`
  - `flex: 1` - 占据剩余空间
  - `justify-content: flex-end` - 右对齐

### 选择器

- `.filter-picker.compact` - 紧凑选择器
  - `padding: 12rpx 20rpx` - 减小内边距
  - `min-width: 100rpx` - 减小最小宽度
  - `gap: 8rpx` - 文字和箭头间距

### 分隔符

- 从"至"改为"-"
- 更简洁，占用空间更小

## 日期格式优化

### 按年模式

- 格式：`2024年`
- 保持不变，清晰明了

### 按月模式

- 改进前：`2024年1月`
- 改进后：`2024/1`
- 更紧凑，节省空间

## 响应式适配

筛选条件会自动适应容器宽度：

- 统计粒度固定在左侧
- 时间范围自动占据剩余空间
- 在小屏幕上也能正常显示

## 代码修改

### WXML 修改

1. 移除 `filter-label` 标签
2. 将 `filter-row` 改为 `filter-row-compact`
3. 将 `time-range` 改为 `time-range-compact`
4. 月份格式从 `{{year}}年{{month}}月` 改为 `{{year}}/{{month}}`
5. 分隔符从"至"改为"-"

### LESS 修改

1. 新增 `.filter-row-compact` 样式
2. 新增 `.time-range-compact` 样式
3. 新增 `.filter-picker.compact` 样式
4. 移除旧的 `.filter-row`、`.filter-label` 等样式

## 优势

1. **节省空间** - 从两行减少到一行
2. **更简洁** - 去掉冗余文字
3. **更直观** - 一眼看清所有筛选条件
4. **更紧凑** - 与图表宽度一致
5. **更现代** - 符合现代UI设计趋势

## 兼容性

- ✅ 支持按年/按月切换
- ✅ 支持时间范围选择
- ✅ 支持级联选择器
- ✅ 保持所有原有功能
- ✅ 无诊断错误

## 视觉效果

筛选条件现在：

- 与下方图表卡片宽度一致
- 视觉上更统一
- 减少了视觉噪音
- 提升了整体美观度
